package com.wojtek.Metaprogramming.Runtime

// MetaProgramming is the writing of computer programs that write or manipulate other programs or themselves

// Meta Object Protocol (MOP)

// MOP is a collection of rules of how a request for a method call is handled
// by the Groovy runtime system and how to control the intermediate layer

// POJO - plain old java object , class can be written in Java or any other language for JVM

// POGO - a Groovy obejct, class written in Groovy. EXTENDS java.lang.Object and
// IMPLEMENTS groovy.lang.GroovyObject interface

// Groovy Interceptor - Groovy object that implements the groovy.lang.GroovyInterceptable interface
// and has method - interception capability

// Customizing MOP with hooks

